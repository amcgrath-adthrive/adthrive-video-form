export class SiteSettings {
  siteId: string;
  playerId: string;
  playlistId: string;
  videoTitle: string;
  titleImage: string;
  shuffle: boolean;
  sticky: boolean;
  float: boolean;
}
